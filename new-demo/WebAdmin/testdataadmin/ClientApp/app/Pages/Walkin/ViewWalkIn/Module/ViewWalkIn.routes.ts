import { Routes, RouterModule } from '@angular/router';
import { ViewWalkInComponent } from '../ViewWalkIn.Component';

export const ViewWalkInRoutes: Routes = [
  { path: '', component: ViewWalkInComponent },
  // { path: 'index', component: YuvaComponent }
];
