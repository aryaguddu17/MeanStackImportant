import { Routes, RouterModule } from '@angular/router';
import { WalkinListComponent } from '../WalkinList.Component';

export const WalkinListRoutes: Routes = [
  { path: '', component: WalkinListComponent },
];
