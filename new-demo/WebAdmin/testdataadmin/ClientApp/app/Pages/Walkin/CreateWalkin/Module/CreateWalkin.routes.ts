import { Routes, RouterModule } from '@angular/router';  
import { CreateWalkinComponent } from '../CreateWalkin.Component';


export const CreateWalkinRoutes: Routes = [
  { path: '', component: CreateWalkinComponent },
  // { path: 'index', component: YuvaComponent }
];
