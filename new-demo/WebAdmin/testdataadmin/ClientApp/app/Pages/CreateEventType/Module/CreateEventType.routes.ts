import { Routes, RouterModule } from '@angular/router';
import {CreateEventTypeComponent } from '../CreateEventType.component';

export const CreateEventTypeRoutes: Routes = [
  { path: '', component: CreateEventTypeComponent }
];

