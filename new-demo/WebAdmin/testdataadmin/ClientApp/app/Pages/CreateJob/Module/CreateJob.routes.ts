import { Routes, RouterModule } from '@angular/router';  
import { CreateJobComponent } from '../CreateJob.Component';

export const CreateJobRoutes: Routes = [
  { path: '', component: CreateJobComponent },
];
