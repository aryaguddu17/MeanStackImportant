import { Routes, RouterModule } from '@angular/router';
import { ViewJobComponent } from '../ViewJob.Component';

export const ViewJobRoutes: Routes = [
  { path: '', component: ViewJobComponent },
  // { path: 'index', component: YuvaComponent }
];
