import { Routes, RouterModule } from '@angular/router'; 
import {InterviewListComponent } from '../InterviewList.component';
 
export const InterviewListRoutes: Routes = [
  { path: '', component: InterviewListComponent }
];
  
