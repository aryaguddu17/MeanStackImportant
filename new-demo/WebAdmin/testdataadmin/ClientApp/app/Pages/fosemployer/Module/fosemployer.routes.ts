import { Routes, RouterModule } from '@angular/router';
import { FosemployerComponent } from '../fosemployer.component';

export const FosEmployerRoutes: Routes = [
  { path: '', component: FosemployerComponent }
];

