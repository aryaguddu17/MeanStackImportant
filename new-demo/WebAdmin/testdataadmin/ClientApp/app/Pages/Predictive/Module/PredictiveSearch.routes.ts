import { Routes, RouterModule } from '@angular/router'; 
import { PredictiveSearchComponent } from "../PredictiveSearch.component";
export const PredictiveSearchRoutes: Routes = [
  { path: '', component: PredictiveSearchComponent }
];
  
